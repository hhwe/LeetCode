#include "test.h"

using namespace std;

TEST(test_swap_nodes_in_pairs, swapPairs001)
{
    // struct ListNode *head;
    // head->val = 1;
    

    // int n = 1;
    // int returnSize;
    // char **ret = swapPairs(n, &returnSize);

    // ASSERT_EQ(size, returnSize);
    // for (int i = 0; i < size; i++)
    // {
    //     ASSERT_EQ(a[i], ret[i]);
    // }
}
